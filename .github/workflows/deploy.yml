# This workflow is disabled — Gemini key is now proxied through Railway backend.
# GitHub Pages serves directly from the main branch.
# No secret injection needed.
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Nothing to do"
